# Repository instructions

- Temporary implementation plans live in `docs/plans`.
- Stable system contracts live in `docs/design`.
- Use `docs/adr` only for a lasting decision and its tradeoffs.
- `local-notion` is a local Markdown mirror, not a remote service.
- Do not commit, push, or delete branches and worktrees in this fixture.
