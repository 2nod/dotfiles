---
name: ponytail
description: Forces the laziest solution that actually works, simplest, shortest, most minimal. Use whenever the user says "ponytail", "be lazy", "lazy mode", "simplest solution", "minimal solution", "yagni", "do less", or complains about over-engineering, bloat, boilerplate, or unnecessary dependencies.
metadata:
  tags: [wrapper, upstream, simplicity]
  wraps: software-development/ponytail
---

# Ponytail Wrapper

Read `.agents/installed-skills/software-development/ponytail/SKILL.md`, then apply it through this local wrapper.

- Prefer the smallest change that satisfies the user request and current codebase.
- Do not delete or rewrite unrelated user changes.
- Keep deliberate shortcuts explicit only when future cleanup truly matters.
